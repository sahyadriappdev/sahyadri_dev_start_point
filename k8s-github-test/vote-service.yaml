apiVersion: v1
kind: Service
metadata:
  labels:
    app: vote
  name: vote
  namespace: github-testing
spec:
  type: NodePort
  ports:
  - name: "vote-service"
    port: 8080
    targetPort: 80
    nodePort: 31004
  selector:
    app: vote
  
